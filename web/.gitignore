node_modules/
dist/
.astro/
.cache/
.wrangler/

# web-export (Rust) の出力。`npm run export` が再生成する
data/
# core-coder が Rust から出す本番テーマ CSS (生成物)
public/themes.css
# wasm-pack の出力 (public/ に置くので実行時 URL /fold/… で取りに行ける)
public/fold/
