---
/** ImasTagChip の Web 版 — セトリ行のユニット表記 / カバー / オリメン / 衣装、一覧の種別・主演・ゲスト。 */
interface Props {
  text: string;
  kind: "unit" | "all" | "cover" | "lead" | "guest" | "lineup" | "first" | "costume";
}
const { text, kind } = Astro.props;
---

<span class:list={["tag", `tag--${kind}`]}>{text}</span>
