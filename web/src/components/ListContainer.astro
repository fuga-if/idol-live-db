---
/** ImasListContainer 相当 — inset-grouped 風のカード。行の間に自動で罫が入る。 */
interface Props {
  as?: "div" | "ul" | "ol";
}
const { as: Tag = "div" }: Props = Astro.props;
---

<Tag class="card"><slot /></Tag>
