// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { Ref } from "./Ref";
import type { SeoBlock } from "./SeoBlock";
import type { StatTile } from "./StatTile";

/**
 * `/brands/<id>/` の中身。
 */
export type BrandPage = { schemaVersion: number, id: string, path: string, name: string, shortName: string | null, themeKey: string, 
/**
 * そのブランドの数の帯 (ライブ / 楽曲 / アイドル / ユニット)。押すと一覧へ。
 *
 * トップの件数タイルと同じ形。**一覧を作っていない組み合わせは
 * そもそも並ばない** (`other` はアイドルだけ) ので、リンク切れにならない。
 */
statTiles: Array<StatTile>, idols: Array<Ref>, units: Array<Ref>, recentEvents: Array<Ref>, topSongs: Array<Ref>, seo: SeoBlock, };
