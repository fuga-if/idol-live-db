<?xml version="1.0" encoding="utf-8"?>
<!--
  チケット締切。日付 + イベント名を 3 行なので、横幅が無いと名前が読めない。
  最小幅を広めに取り、縦だけリサイズ可にしてある (iOS も systemMedium のみ)。

  名前は AndroidManifest の receiver の android:label、一覧に出る説明文は
  android:description が持つ (この属性は @string/… しか受け付けない)。
-->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:description="@string/widget_desc_ticket_deadline"
    android:minWidth="250dp"
    android:minHeight="110dp"
    android:minResizeWidth="250dp"
    android:minResizeHeight="110dp"
    android:targetCellWidth="4"
    android:targetCellHeight="2"
    android:resizeMode="vertical"
    android:widgetCategory="home_screen"
    android:updatePeriodMillis="0"
    android:initialLayout="@layout/glance_default_loading_layout" />
